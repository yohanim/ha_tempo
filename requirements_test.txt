# Full integration test stack (Home Assistant + pytest). Mirrors ha_daikin_local.
#
# Install: python -m pip install -r requirements_test.txt
# Run: python -m pytest -v
#
# Pin tracks homeassistant used by pytest-homeassistant-custom-component; bump
# when you bump hacs.json / Core target.
pytest-homeassistant-custom-component==0.13.320

# pytest 9 + async harness (explicit so the plugin is always available to the runner).
pytest-asyncio>=1.3.0

aioresponses>=0.7.6
babel
